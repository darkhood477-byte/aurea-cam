sed -i 's/val cameraSelector = remember(lensFacing) { CameraSelector.Builder().requireLensFacing(lensFacing).build() }/val cameraSelector = remember(lensFacing) {\n        try {\n            CameraSelector.Builder().requireLensFacing(lensFacing).build()\n        } catch(e: Exception) {\n            CameraSelector.DEFAULT_BACK_CAMERA\n        }\n    }/g' app/src/main/java/com/example/camera/CameraScreen.kt
